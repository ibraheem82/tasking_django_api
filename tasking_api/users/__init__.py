# specifies the default configuration class ('users.apps.UsersConfig') for a Django app.
# * 'users.apps.UsersConfig' refers to the configuration class defined in the apps.py file within the 'users' app.
# The UsersConfig class likely contains settings related to the app (e.g., app name, verbose name, signals, etc.).
default_app_config = 'users.apps.UsersConfig' # It tells Django which configuration class to use when initializing the app.

